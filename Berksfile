source "https://supermarket.getchef.com"

# To pull a specific cookbook from Github, instead of Supermarket, add a line like this, e.g.
#cookbook "tsmclient", :git => "https://git.yale.edu/Chef-YaleITS/tsmclient.git"

metadata
